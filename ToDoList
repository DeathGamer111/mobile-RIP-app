Fix SVG anf PDF Validation
Fix Save and PRN Output to one single file at time
CLean up code and add comments
Make the property ImageMeta the variable used instead of meta in JobsDetailsView
Fix image preview to remove the edges of the rectangle if the image is smaller than the bounding area
